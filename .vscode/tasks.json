{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "publish",
            "args": [
                "dotnet publish CartApi.csproj -c Debug -o ./obj/Docker/publish"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}